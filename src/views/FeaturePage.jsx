import { Typography } from "@mui/material";

/**
 * An empty view component to start with
 */
export default function FeaturePage() {
  return (
    <Typography>
      I may look like an empty component, but I am full of potential.
    </Typography>
  );
}
